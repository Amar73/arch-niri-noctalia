# Логи
error.log
*.log

# Бэкапы dotfiles (создаются install.sh и sync.sh)
*.bak.*

# Временные файлы
*.tmp
*~
.DS_Store

# Прогресс-файл установки (машиноспецифичный)
# Не трекать — у каждой машины свой ~/.install-progress
# (файл хранится в REPO_HOME, не в репозитории)

# Бэкап-директории (создаются backup.sh)
backup/

# Темы alacritty (клонируются отдельно при install/sync)
files/home/.config/alacritty/themes/
